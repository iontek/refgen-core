# Copy to .env and adjust. The shared JWT secret must match the platform's
# djangorestframework-simplejwt signing key once identity-svc issues tokens.
JWT_SECRET=dev-insecure-change-me
JWT_ALGORITHM=HS256
LOG_LEVEL=INFO
